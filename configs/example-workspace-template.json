// Comments are allowed: lines starting with // or # are ignored by the loader.
{
  "vars": {
    "TOOLDIR": "${TOOLDIR}",
    "WORKSPACE": "${WORKSPACE}",
    "kernel_src": "${WORKSPACE}/linux",
    "kernel_config": "${WORKSPACE}/kernel.config",
    "build_dir": "${WORKSPACE}/build",
    "logs_dir": "${WORKSPACE}/logs",
    "modules_list": "${WORKSPACE}/modules.txt",
    "profile_dir": "${TOOLDIR}/configs/profiles",
    "rule_dir": "${TOOLDIR}/configs/rules"
  },
  "project": {
    "name": "example-workspace-template",
    "work_dir": "${WORKSPACE}/work-example-workspace-template"
  },
  "kernel": {
    "source_dir": "${kernel_src}",
    "rev_old": "v4.14.206",
    "rev_new": "v4.14.336"
  },
  "inputs": {
    "kernel_config": "${kernel_config}",
    "build_dir": "${build_dir}",
    "kernel_build_log": "${logs_dir}/kernel-build.log",
    "yocto_build_log": "${logs_dir}/yocto.log",
    "modules_file": "${modules_list}",
    "dts_roots": [
      "${kernel_src}/arch/arm/boot/dts"
    ]
  },
  "profiles": {
    "profile_root": "${profile_dir}",
    "rule_root": "${rule_dir}"
  },
  "active_profiles": [
    "tcu_modem_connectivity"
  ],
  "templates": {
    "report_title": "Kernel commit analysis report",
    "html_summary": true,
    "top_n_commits": 50,
    "include_pipeline_stats": true
  }
}
