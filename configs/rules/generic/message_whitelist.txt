# Comments are allowed: lines starting with # are ignored by the rules parser.
# substring
Fixes:
# wildcard
*regression*
CVE-*
syzbot
*latency*
*throughput*
# regex
re:(?i)use-after-free|out-of-bounds|overflow
