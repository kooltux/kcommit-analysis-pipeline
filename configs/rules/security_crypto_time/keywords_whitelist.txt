# Pattern syntax — one pattern per line:
#   keyword    plain substring match (case-insensitive)   e.g. overflow
#   glob       wildcard: * = anything, ? = one char       e.g. CVE-*
#   re:REGEX   regular expression (re: prefix required)   e.g. re:(?i)cve-\d+
# Inline comments: text after whitespace + # is stripped  e.g. pattern  # note
constant-time
constant time
side channel
side-channel
timing leak
timing side channel
crypto
cipher
encryption
decryption
mac
hmac
digest
hash
key material
key leakage
