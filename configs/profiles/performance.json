// Comments are allowed: lines starting with // or # are ignored by the loader.
{
  "name": "performance",
  "description": "Performance, latency, and throughput improvements.",
  "rules": [
    "performance_general",
    "performance_latency",
    "performance_throughput"
  ]
}
