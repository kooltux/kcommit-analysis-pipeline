// Comments are allowed: lines starting with // or # are ignored by the loader.
{
  "name": "security_features",
  "description": "New security features, hardening changes, and capability/policy updates.",
  "rules": {
    "generic": 40,
    "security_general": 70,
    "security_auth_caps": 80,
    "security_crypto_time": 80,
    "security_syscalls": 60
  }
}
